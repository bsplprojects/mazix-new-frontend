/* eslint-disable prettier/prettier */
export type Member = {
  id: string;
  name: string;
  placementId: string;
  joinDate: string;
  leg: string;
  bv: number;
  active: boolean;
  rank: string;
};